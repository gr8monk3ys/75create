import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Set up your challenge' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
