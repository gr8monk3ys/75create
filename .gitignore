/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel

# test artifacts
.playwright-mcp/
/*.png

# playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Impeccable live-mode runtime state (server port/pid, sessions)
/.impeccable/live/

# local secrets (see .env.example)
.env*.local
.env

# Agent skills are installed per machine, not vendored
.claude/
